/* KSL V6.3 — staged expiry report flow + multi-template */
(()=>{'use strict';if(window.__KSL_FLOW_MULTI_V63__)return;window.__KSL_FLOW_MULTI_V63__=1;
const F='KSL_EXPIRY_FLOW_MULTI_V63',K='KSL_EXPIRY_CUSTOM_TEMPLATES_V636',q=v=>String(v??'').trim(),st=()=>{try{if(typeof appState!=='undefined'&&appState)return appState}catch(_){}return window.appState||null},msg=(m,t='')=>{try{if(typeof toast==='function')return toast(m,t)}catch(_){}alert(m)},wait=n=>new Promise(r=>setTimeout(r,n));
const getF=()=>{try{return JSON.parse(localStorage.getItem(F)||'{}')||{}}catch(_){return{}}},setF=x=>{try{localStorage.setItem(F,JSON.stringify(x))}catch(_){}},hdr=()=>({branch:q(document.getElementById('eaBranch')?.value),checkedAt:q(document.getElementById('eaCheckedAt')?.value),shiftEmployee:q(document.getElementById('eaShiftEmployee')?.value),inspector:q(document.getElementById('eaInspector')?.value)}),valid=h=>!!(h?.branch&&h?.checkedAt&&h?.shiftEmployee&&h?.inspector);
function style(){if(document.getElementById('kslFlowMulti63Css'))return;let s=document.createElement('style');s.id='kslFlowMulti63Css';s.textContent=`#expiryAudit #eaFlowStart{display:none}#expiryAudit #eaFlowStart.ready{display:inline-flex}#expiryAudit:not(.flow63-on) #eaPickerPanel,#expiryAudit:not(.flow63-on) .ea-table-card,#expiryAudit:not(.flow63-on) #eaHistorySave,#expiryAudit:not(.flow63-on) #eaHistoryNew{display:none!important}#expiryAudit.flow63-on #eaHistoryNew{display:inline-flex!important}.flow63-start{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:9px}.flow63-note{font-size:9px;color:#71847b;font-weight:800}.flow63-note.ok{color:#11784d}.flow63-box{margin:0 0 9px;padding:9px;border:1px solid #cfe4da;border-radius:10px;background:#f8fcfa}.flow63-list{display:flex;gap:6px;flex-wrap:wrap;max-height:150px;overflow:auto}.flow63-chip{display:inline-flex;gap:6px;align-items:center;padding:6px 9px;border:1px solid #cfe2d8;border-radius:999px;background:#fff;font-size:9px;font-weight:800}.flow63-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap;margin-top:7px}#expiryAudit .ea-custom-row #eaTemplateSelect,#expiryAudit .ea-custom-row #eaTemplateApply{display:none!important}`;document.head.appendChild(s)}
async function saveFlow(f){setF(f);let s=st();if(!s)return false;s.expiryAuditCurrentInspection=JSON.parse(JSON.stringify(f));try{if(typeof dbSet==='function'){await Promise.resolve(dbSet(s));return true}}catch(e){console.warn('[KSL] flow save failed',e)}return false}
function restore(){let f=getF(),o=st()?.expiryAuditCurrentInspection;if(o?.active&&valid(o.header)){f=o;setF(f)}if(f?.active&&valid(f.header)){for(const [id,v] of Object.entries({eaBranch:f.header.branch,eaCheckedAt:f.header.checkedAt,eaShiftEmployee:f.header.shiftEmployee,eaInspector:f.header.inspector})){let e=document.getElementById(id);if(e&&!q(e.value))e.value=q(v)}}}
function stage(){let p=document.getElementById('expiryAudit');if(!p)return;let f=getF(),h=hdr(),on=!!(f.active&&valid(f.header));p.classList.toggle('flow63-on',on);let b=document.getElementById('eaFlowStart'),n=document.getElementById('eaFlowNote');if(b){b.classList.toggle('ready',valid(h)&&!on);b.disabled=!valid(h)||on}if(n){n.classList.toggle('ok',valid(h));n.textContent=on?'ข้อมูลการตรวจถูกบันทึกแล้ว — เลือกวัตถุดิบหรือ Template เพื่อเริ่มตรวจ':valid(h)?'ข้อมูลครบแล้ว สามารถสร้างรายงานใหม่ได้':'กรอกข้อมูลการตรวจให้ครบ 4 ช่อง'}let old=document.getElementById('eaHistoryNew');if(old){old.textContent='＋ รายงานถัดไป';old.title='จบรายงานปัจจุบันและเริ่มรายงานถัดไป'}if(on)setTimeout(tplUI,50)}
async function start(){let h=hdr();if(!valid(h)){msg('กรุณาลงข้อมูลการตรวจให้ครบทั้ง 4 ช่อง','warn');return}let f={id:'EA-DRAFT-'+Date.now().toString(36),active:true,header:h,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};document.getElementById('eaClearSelected')?.click();setF(f);stage();let online=await saveFlow(f);msg(online?'สร้างรายงานใหม่และบันทึกข้อมูลการตรวจ Online แล้ว':'สร้างรายงานใหม่แล้ว และบันทึกข้อมูลการตรวจในเครื่อง','success');document.getElementById('eaPickerPanel')?.scrollIntoView({behavior:'smooth',block:'start'})}
let timer;function auto(){let f=getF();if(!f.active)return;clearTimeout(timer);timer=setTimeout(()=>{let h=hdr();if(valid(h)){f.header=h;f.updatedAt=new Date().toISOString();saveFlow(f)}},350)}
function tpls(){let a=[];try{let x=JSON.parse(localStorage.getItem(K)||'[]');if(Array.isArray(x))a=x}catch(_){}let b=Array.isArray(st()?.expiryAuditTemplates)?st().expiryAuditTemplates:[],m=new Map();[...a,...b].forEach(x=>{let id=q(x?.id);if(id){let o=m.get(id);if(!o||String(x.updatedAt||'')>=String(o.updatedAt||''))m.set(id,x)}});return [...m.values()].sort((a,b)=>q(a.name).localeCompare(q(b.name),'th'))}
const selected=()=>[...document.querySelectorAll('#eaFlowTplList input:checked')].map(x=>q(x.value));
function syncLegacy(){let a=selected(),s=document.getElementById('eaTemplateSelect');if(s){s.value=a.length===1?a[0]:'';s.dispatchEvent(new Event('change',{bubbles:true}))}for(const id of ['eaTemplateEdit','eaTemplateDelete']){let e=document.getElementById(id);if(e)e.disabled=a.length!==1}let c=document.getElementById('eaFlowTplCount');if(c)c.textContent=a.length?`เลือก ${a.length} Template`:'ยังไม่ได้เลือก Template'}
function tplUI(){let c=document.getElementById('eaCustomTemplates');if(!c)return false;let b=document.getElementById('eaFlowTplBox');if(!b){b=document.createElement('div');b.id='eaFlowTplBox';b.className='flow63-box';b.innerHTML='<div style="display:flex;justify-content:space-between;gap:8px;flex-wrap:wrap;margin-bottom:7px"><b>เลือก Template สำหรับการตรวจ</b><small class="flow63-note">เลือกได้หลาย Template พร้อมกัน ระบบจะรวมรายการซ้ำให้อัตโนมัติ</small></div><div id="eaFlowTplList" class="flow63-list"></div><div class="flow63-actions"><button id="eaFlowTplApply" type="button" class="btn btn-primary">ใช้ Template ที่เลือก</button><span id="eaFlowTplCount" class="flow63-note">ยังไม่ได้เลือก Template</span></div>';let r=c.querySelector('.ea-custom-row');r?c.insertBefore(b,r):c.prepend(b)}let keep=new Set(selected()),a=tpls(),l=document.getElementById('eaFlowTplList');l.innerHTML=a.length?a.map(x=>`<label class="flow63-chip"><input type="checkbox" value="${q(x.id)}" ${keep.has(q(x.id))?'checked':''}><span>${q(x.name)||'Custom Template'} • ${(Array.isArray(x.items)?x.items.length:0)} รายการ</span></label>`).join(''):'<span class="flow63-note">ยังไม่มี Template — กด ＋ Custom เพื่อสร้างกลุ่มสินค้า</span>';syncLegacy();return true}
async function apply(){let a=selected();if(!a.length){msg('กรุณาเลือก Template อย่างน้อย 1 รายการ','warn');return}let ts=tpls().filter(x=>a.includes(q(x.id))),keys=[...new Set(ts.flatMap(x=>Array.isArray(x.items)?x.items.map(q):[]).filter(Boolean))];document.getElementById('eaClearSelected')?.click();await wait(50);let p=document.getElementById('eaItemPicker');if(!p){msg('ยังไม่พบ Drop Down วัตถุดิบ','warn');return}let n=0;for(const k of keys){if(![...p.options].some(o=>q(o.value)===k))continue;p.value=k;p.dispatchEvent(new Event('change',{bubbles:true}));n++;await wait(10)}msg(`เลือก ${ts.length} Template รวม ${n} รายการ`,'success')}
function ui(){let p=document.getElementById('expiryAudit'),a=p?.querySelector('.ea-form .ea-actions');if(!p||!a)return false;style();if(!document.getElementById('eaFlowStart'))a.insertAdjacentHTML('afterend','<div class="flow63-start"><button id="eaFlowStart" type="button" class="btn btn-primary">＋ สร้างรายงานใหม่</button><span id="eaFlowNote" class="flow63-note">กรอกข้อมูลการตรวจให้ครบ 4 ช่อง</span></div>');let sv=document.getElementById('eaHistorySave');if(sv)sv.textContent='💾 บันทึกรายงานผลตรวจ';restore();stage();tplUI();return true}
let tries=0;function boot(){tries++;if(!ui()&&tries<60)setTimeout(boot,150)}
document.addEventListener('input',e=>{if(['eaBranch','eaCheckedAt','eaShiftEmployee','eaInspector'].includes(e.target?.id)){stage();auto()}},true);document.addEventListener('change',e=>{if(['eaBranch','eaCheckedAt','eaShiftEmployee','eaInspector'].includes(e.target?.id)){stage();auto()}if(e.target?.closest?.('#eaFlowTplList'))syncLegacy()},true);document.addEventListener('click',e=>{if(e.target.closest?.('#eaFlowStart')){e.preventDefault();start();return}if(e.target.closest?.('#eaFlowTplApply')){e.preventDefault();apply();return}if(e.target.closest?.('#eaHistoryNew')){setF({});setTimeout(()=>{stage();tplUI()},120);return}if(e.target.closest?.('[data-eah-load]'))setTimeout(()=>{let h=hdr();if(valid(h)){let f={id:'EA-EDIT-'+Date.now().toString(36),active:true,header:h,updatedAt:new Date().toISOString()};setF(f);stage()}},600);if(e.target.closest?.('#eaTemplateSave,#eaTemplateDelete,#eaTemplateNew,#eaTemplateEdit,.nav button[data-page="expiryAudit"],#eaRefresh'))setTimeout(()=>{ui();tplUI()},200)},true);window.addEventListener('ksl-central-synced',()=>setTimeout(()=>{ui();tplUI()},120));boot();console.info('[KSL] V6.3 staged flow + multi-template ready');})();
